# 06 — 开发任务清单（ai-pet-app）

## Epic A — 工程与壳

- [x] A1 建 Vue 3 + Vite + TS 工程（PWA 用 vite-plugin-pwa，可后置）
- [x] A2 路由、状态管理（Pinia）、网络层（axios/fetch 封装 + JWT）、Token 存储
- [x] A3 主题与手机/桌面响应式导航壳（断点见 docs/04）
- [x] A4 可配置 API_BASE（内测，`VITE_API_BASE`）

## Epic B — 账号与设备（V0.2）

- [x] B1 登录/注册  
- [x] B2 设备列表/绑定/详情（“详情”目前为首页卡片摘要级，无独立详情页）  
  - [x] B2.1-legacy 手动输入 `device_uid`（MAC）绑定过渡实现；已由正式认领替换，不得恢复
  - [x] B2.1 正式绑定：输入/扫码 `binding_id` 认领（处理 403/404/409/422）
  - [x] B2.2 设备列表、当前设备选择与详情摘要（`GET /devices`）
  - [x] B2.3 设备改名/解绑（`PATCH /devices/{id}`、`DELETE /devices/{id}`；首页设备列表行内改名 + 解绑二次确认）
- [ ] B3 配网引导页（图文）  

## Epic C — 人设与资产（V0.2）

- [x] C1 人设设置（星座+MBTI+忌口+钉扎；E2 GET/PUT 已接入）  
- [x] C2 记忆列表/新建/删除/候选审核（memories CRUD + approve/reject 已接入）
- [x] C3 历史按日浏览/删除（E4 GET/DELETE 已接入）  
- [x] C4 首页宠物卡片（当前设备人设摘要；`GET /devices/{id}/persona` 已接入）
- [x] C5 我的星仔角色档案（dossier 六字段全部可见可编辑；未配人设引导先去 C1）
- [x] C6 人设问卷入口（`GET/POST /persona/questionnaire`；不算型、不替代直选）

## Epic D — 表达与内容（V0.3～V1.0）

- [x] D1 外设状态页（`GET /devices/{id}/peripheral` 已接入：快照、空状态与手动刷新）
- [x] D2 日运/小记/摘要（读取 `daily_summary` analyses，兼容等待生成空态）
- [x] D5 人设成长建议卡（人设页内读取 `persona_growth` analyses 最近一条；「应用建议」二次确认后调 `POST /devices/{id}/analyses/{aid}/apply-persona-growth`，成功后刷新人设与卡片状态）  
- [x] D6 运势卡片（看板 A13；日运页内读取 `GET /devices/{id}/fortune/daily`：星座五维度 + greeting + 已生成时的八字运势，`generating=true` 走「生成中」空态，404 引导先配置人设；手动刷新）  
- [x] D7 主人八字录入（看板 A14；人设页内独立卡片，接入 `GET/PUT /devices/{id}/bazi`：阳历/阴历 + 出生日期 + 时辰（可未知）+ 出生地 + 性别；未录入 GET 404 为空表单，422 给中文提示，保存成功提示去日运页看八字运势）
- [x] 趣味测试 + 简略星盘 + 结果海报分享（`/tests` 作答，`/tests/result` 独立结果页；海报配色跟结果走并带 PWA 二维码；应用名暂定「守护星」）  
- [x] D3 导出我的数据（`POST /devices/{id}/export` 同步 JSON 包，我的页摘要 + 本地下载）
- [x] D4 多设备切换（实质功能已在 B2.2 落地：当前设备选择 + localStorage 持久化 + 首页列表切换，见 `src/stores/devices.ts`、`src/views/HomeView.vue`）
- [x] D8 记忆画像卡（记忆页读取 `analyses?kind=memory_profile`）  

## Epic E — 桌面增强

- [ ] E1 宽屏分栏（历史 | 记忆）  
- [ ] E2 窗口尺寸记忆  
- [ ] E3 （可选）托盘与开机启动  

## Epic F — 发布

- [ ] F1 PWA 上线（HTTPS 部署，URL 分发给内测用户）
- [ ] F2 开启可安装（manifest / 离线缓存）与桌面快捷方式（manifest + autoUpdate SW 配置已在 `vite.config.ts` 落地；因 HTTPS 未就绪暂不勾选）
- [ ] F3 （后置）需要推送/上架时用 Capacitor 出 Android/iOS 包
- [ ] F4 与 backend 联调验收（产品 §8.2 用户向用例）

## 完成定义（用户端 V0.2）

手机与桌面均可：登录 → 绑设备 → 定双鱼+MBTI → 查看并管理记忆/历史；数据与 admin 所见一致（同源 API）。  

## 当前开发进度与后续计划（2026-08-18）

### 已完成

- 已上线并接入真实用户 API：B1 登录注册、B2.1 `binding_id` 认领、B2.2 设备选择、B2.3 设备改名/解绑、C1 人设读写、C2 记忆管理、C3 历史、C4 首页人设摘要、C5 我的星仔档案、C6 人设问卷、D1 外设快照、D2 服务端小记、D3 数据导出、D4 多设备切换、D5 人设成长建议卡、D6 运势卡片、D7 主人八字录入、D8 记忆画像卡。
- C4 首页人设摘要已完成：仅展示当前选中设备的后端人设，不在客户端推断或生成。
- C5：用户拍板 dossier 六字段全部可见可编辑，不再等产品边界。
- C6：问卷只展示后端题面并提交 a/b，不在客户端算 MBTI，不撤直选。
- D3：直接消费 `POST /export` JSON，本地下载；不含 MAC 与生辰原文。
- D8：只读最新一条 `memory_profile`，等待生成 / 空画像为空态。

### 已确认但暂不具备实现条件

- 人设生效第四态「已验证生效」（看板 A3）：截至 2026-08-18，backend 的 `PersonaProfile` 模型与 `GET/PUT /devices/{id}/persona` 响应均无人设生效状态字段（仅 `follow_latest`/`kb_version` 两个派生态），契约 docs/06 亦无第四态枚举。已在协作看板向 backend 提出补字段与枚举需求；后端上线后再做前端徽章。

- B3 配网引导：等待固件确认最终配网步骤、二维码/图文素材及失败恢复口径。
- F1 正式发布：当前仅 HTTP 内测，须完成域名、HTTPS 和分发后再开放可安装 PWA。

### 推荐顺序

1. 以真实账号和已认领设备验收本轮 C5/C6/D3/D8，以及此前已提交但公网未生效的改名/解绑、成长建议、运势与八字。
2. 固件交付配网素材后开发 B3；backend 补齐人设生效状态后再做 A3 徽章。
3. 进行宽屏布局和窗口尺寸记忆（E1/E2），并完成 HTTPS、PWA 安装和正式联调（F1/F2/F4）。
