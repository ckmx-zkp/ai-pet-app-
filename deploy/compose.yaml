services:
  app-web:
    image: nginx:1.27-alpine
    container_name: ai-pet-app-web
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
